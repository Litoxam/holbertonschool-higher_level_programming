#!/usr/bin/node

const array = [ // stocks data into an array
  'C is fun',
  'Python is cool',
  'JavaScript is amazing'
];

let i = 0;
while (i < array.length) { // parse the array with an index
  console.log(array[i]);
  i++;
}
